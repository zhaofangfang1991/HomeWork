#include <stdio.h>

int main(int argc, char *argv[])
{
  /* char buf[100] = {0};
   printf("qingshu f");
   scanf("%s",buf);


   char tmp[100] = {0};
   printf("shuru  p");
   scanf("%s",tmp);
   printf("tmp = %s\n",tmp);

   char str[10];
   printf("shur str  ");
   scanf("%s",str);
   printf("str  %s\n", str);

   char buf[100];
   gets(buf);
   printf("buf  =  %s\n",buf);


   char buf[100];
   fgets(buf, sizeof(buf), stdin);
   printf("buf  = @ %s $\n",buf);

    char buf[] = "hello";
    fputs(buf,stdout);*/


    char buf[] = "hello";
    puts(buf);
    printf("gggg   %s",buf);
















    return 0;
}
